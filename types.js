// types.js - JavaScript version without TypeScript types

// These are the only values needed at runtime
// The rest were TypeScript type definitions that don't exist at runtime

// Constants that might be referenced in the code
export const DEFAULT_API_KEY_STORAGE = 'session';
export const API_KEY_STORAGE_KEY = 'brainspire-gemini-api-key';
export const API_KEY_VALIDATION_INTERVAL = 86400000; // 24 hours in milliseconds
